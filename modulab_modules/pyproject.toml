[project]
name = "modulab-pylabware-wrappers"
version = "0.1.0"
description = "Modulab module wrappers for PyLabware drivers"
requires-python = ">=3.10"
dependencies = [
  "PyLabware",
]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
